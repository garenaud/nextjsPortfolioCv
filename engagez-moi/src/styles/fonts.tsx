import { Open_Sans, Average_Sans } from 'next/font/google';

export const openSans = Open_Sans({ weight: ['400', '700'], subsets: ['latin'] });
export const averageSans = Average_Sans({ weight: ['400'], subsets: ['latin'] });
